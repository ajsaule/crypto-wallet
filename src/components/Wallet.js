import React from 'react';
import './Wallet.css';

const Wallet = ({key}) => {
  return (
    <div key={key} className="wallet-wrapper">
      <div>Hello</div>
      <div>World</div>
    </div>
  );
};

export default Wallet;
